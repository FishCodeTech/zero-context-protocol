# Published Benchmark Presentation Artifacts

This repository is the presentation layer for published benchmark summaries.

The benchmark runners and raw benchmark implementation live in
`zero-context-protocol-python`.

Keep only curated, release-worthy benchmark artifacts here. Do not treat local
checkpoint outputs or intermediate smoke runs as public evidence.
